# .hermes.md — cTrader V2

> Regras operacionais. Personalidade = SOUL.md. Docs = specs/.

## REGRAS (RULES.md R1-R10)

- **R1 — Spec-driven**: `.md` ANTES de `.py`. Sem spec = sem código.
- **R2 — KISS/YAGNI**: Só o que o sistema pede. Sem abstração "pro futuro".
- **R3 — DDD**: ORQ wireia, SAT executa 1 coisa. >200L = split.
- **R4 — SSOT**: INDEX.md é a verdade. Disco bate com INDEX.
- **R5 — R-NO-WARN**: Zero warns. Tudo `error`. Nada de `--no-verify`.
- **R6 — R-ASCII-OUT**: ASCII puro. Sem emoji/Unicode em output.
- **R7 — R-NO-MCP-BYPASS**: Só F0 toca MCP. Offline = `{online: false}`.
- **R8 — DDD-NAMING**: `orc_<f>.py` (ORQ), `<n>_orc_<p>.py` (SAT).
- **R9 — R-USE**: Reaproveite antes de criar. Pip install = último recurso.
- **R10 — R21**: Não imagine. Pesquise no disco. `grep` antes de afirmar.

## WORKFLOW

1. Ler INDEX.md (SSOT)
2. Espec no `orc_*.md` (PROPOSITO→FLUXO→ORQ→FILHOS)
3. Código no `.py`
4. Harness + gates (G8+G11-G14)
5. Commit com `[NC-CTRADER-NNN]`

## GATES OBRIGATÓRIOS

```
PRE-FLIGHT → G0 (ruff 200+) → G2 (slop) → G3 (mockbuster)
→ G5 (pytest) → G8 (INDEX sync) → G10 (MCP contract)
→ G11-G14 (conformance) → G15 (diff-lateral)
→ G16 (metrics) → G17 (ESLint 0/0) → G18 (oxlint 0/0)
```

## APROVAÇÃO

Usuário aprova antes de qualquer mudança estrutural (DDD, rename, delete, move).
Nada de "já fiz" sem confirmação prévia.
