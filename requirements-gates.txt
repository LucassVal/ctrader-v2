# Dependencias PINADAS dos gates do cTrader (Fase 0 item 0.G1 — reprodutibilidade).
# Instalar no venv do Neocortex:  ../../.venv/Scripts/python.exe -m pip install -r requirements-gates.txt
# Sem isso, um venv novo perde G2/G3 e o gates.sh FALHA alto (FULL-ERR, sem SKIP).
ai-slop-detector==3.8.7
mockbuster==0.1.4
ruff>=0.4
pytest>=8
